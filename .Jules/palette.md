## 2024-05-15 - Focus-visible styles for links and buttons
 **Learning:** Relying purely on browser defaults for focus states isn't enough, especially on custom styled buttons and interactive elements.
 **Action:** Add a `:focus-visible` outline for all interactive elements in `site.css` to improve keyboard navigation accessibility.