## 2024-05-13 - Focus Visible Support
**Learning:** By default, WebKit doesn't show focus rings on many interactive elements unless `:focus-visible` is explicitly styled, leaving keyboard users without visual cues.
**Action:** Add `:focus-visible` to interactive elements like buttons, links, and form fields to ensure keyboard accessibility.
